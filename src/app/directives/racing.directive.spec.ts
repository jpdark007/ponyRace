import { RacingDirective } from './racing.directive';

describe('RacingDirective', () => {
  it('should create an instance', () => {
    const directive = new RacingDirective();
    expect(directive).toBeTruthy();
  });
});
